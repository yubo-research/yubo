# UHDSimpleBE: (1+1)-ES with BehavioralEmbedder + ENN seed selection
# on HumanoidStandup-v5 with MLP(32,16).
# Frozen noise (1 seed) — deterministic episodes.
# After warmup, scores 10 candidate perturbation seeds via UCB and picks the best.
# Everything else matches stand_mlp_simple_baseline.toml for a fair comparison.

[uhd]
env_tag = "stand-mlp:fn"
num_rounds = 100
optimizer = "simple_be"
# perturb = "dim:0.5"
perturb = "dense"
log_interval = 10
noise_seed_0 = 0

be_num_probes = 10
be_num_candidates = 30
be_warmup = 20
be_fit_interval = 10
be_enn_k = 10
